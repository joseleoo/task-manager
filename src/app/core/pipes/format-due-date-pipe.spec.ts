import { FormatDueDatePipe } from './format-due-date-pipe';

describe('FormatDueDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDueDatePipe();
    expect(pipe).toBeTruthy();
  });
});
